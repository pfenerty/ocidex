{{- /*
Per-enricher worker Deployments (ADR-033). One Deployment per entry in
.Values.enricherWorkers, each running a single enricher (cmd/<name>) and claiming
its own enricher_name partition from enrichment_jobs. They replaced the legacy
monolithic enrichment-worker, removed in ocidex-kg5. Shared sizing/env comes from
.Values.enricherWorkerDefaults; an entry may override `resources` to opt out of the
shared sizing without raising it for every other enricher.
*/ -}}
{{- range .Values.enricherWorkers }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ocidex.fullname" $ }}-{{ .name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "ocidex.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ .name }}
spec:
  {{- if not $.Values.keda.enabled }}
  replicas: {{ $.Values.enricherWorkerDefaults.replicas }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "ocidex.selectorLabels" (dict "root" $ "component" .name) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ocidex.podLabels" (dict "root" $ "component" .name) | nindent 8 }}
    spec:
      serviceAccountName: {{ include "ocidex.fullname" $ }}
      terminationGracePeriodSeconds: 360
      {{- with $.Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .name }}
          image: {{ include "ocidex.image" (dict "root" $ "name" .name) }}
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          {{- with $.Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: health
              containerPort: 9090
          resources:
            {{- toYaml (.resources | default $.Values.enricherWorkerDefaults.resources) | nindent 12 }}
          envFrom:
            - secretRef:
                name: {{ $.Values.existingSecret }}
          env:
            - name: NATS_URL
              value: "nats://{{ include "ocidex.fullname" $ }}-nats:4222"
            - name: NATS_STREAM_REPLICAS
              value: {{ $.Values.enricherWorkerDefaults.env.natsStreamReplicas | quote }}
            - name: DATABASE_MAX_CONNECTIONS
              value: {{ $.Values.enricherWorkerDefaults.env.databaseMaxConnections | quote }}
            - name: ENVIRONMENT
              value: {{ $.Values.enricherWorkerDefaults.env.environment | quote }}
            - name: LOG_LEVEL
              value: {{ $.Values.enricherWorkerDefaults.env.logLevel | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9090
            initialDelaySeconds: 5
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 9090
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
{{- end }}
