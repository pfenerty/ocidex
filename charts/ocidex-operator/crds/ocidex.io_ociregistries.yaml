# Generated by make generate-operator — do not edit directly.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: ociregistries.ocidex.io
spec:
  group: ocidex.io
  names:
    kind: OCIRegistry
    listKind: OCIRegistryList
    plural: ociregistries
    shortNames:
    - ocireg
    singular: ociregistry
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OCIRegistry declaratively registers an OCI registry with OCIDex.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OCIRegistrySpec defines the desired state of an OCI registry
              registration.
            properties:
              authSecretRef:
                description: |-
                  AuthSecretRef references a Secret containing "username" and "token" keys for
                  registry authentication. Omit for anonymous access.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              includeUntagged:
                description: IncludeUntagged enables scanning of untagged manifests
                  (zot/harbor/ghcr only).
                type: boolean
              insecure:
                description: Insecure allows HTTP (non-TLS) connections.
                type: boolean
              name:
                description: Name is a human-readable label for the registry.
                type: string
              pollIntervalMinutes:
                description: PollIntervalMinutes is the time between registry polls.
                format: int64
                type: integer
              repositories:
                description: |-
                  Repositories is an explicit list of repositories to walk; bypasses catalog
                  discovery when non-empty.
                items:
                  type: string
                type: array
              repositoryPatterns:
                description: RepositoryPatterns are glob patterns for repositories
                  to ingest. Empty means all.
                items:
                  type: string
                type: array
              scanMode:
                default: poll
                description: ScanMode controls how OCIDex discovers new images.
                enum:
                - poll
                - webhook
                - both
                type: string
              tagPatterns:
                description: TagPatterns are glob patterns or "semver" for tags to
                  ingest. Empty means all.
                items:
                  type: string
                type: array
              type:
                description: Type is the registry implementation.
                enum:
                - docker
                - generic
                - ghcr
                - harbor
                - zot
                type: string
              url:
                description: URL is the registry address (e.g. "zot:5000" or "ghcr.io").
                type: string
              visibility:
                default: private
                description: Visibility controls whether the registry is publicly
                  listed.
                enum:
                - public
                - private
                type: string
            required:
            - name
            - type
            - url
            type: object
          status:
            description: OCIRegistryStatus reflects the observed state of an OCIRegistry.
            properties:
              conditions:
                description: |-
                  Conditions describe the current state of the registry.
                  The Ready condition is True when the registry exists in OCIDex and spec is in sync.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              registryID:
                description: RegistryID is the OCIDex UUID assigned on creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
