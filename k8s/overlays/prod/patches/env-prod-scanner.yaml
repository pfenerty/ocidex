apiVersion: apps/v1
kind: Deployment
metadata:
  name: ocidex-scanner-worker
spec:
  template:
    spec:
      containers:
        - name: scanner-worker
          imagePullPolicy: IfNotPresent
          env:
            - name: LOG_LEVEL
              value: "info"
            - name: ENVIRONMENT
              value: "production"
            - name: NATS_STREAM_REPLICAS
              value: "1"
            - name: SCANNER_MAX_CONCURRENCY
              value: "2"
