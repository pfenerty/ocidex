apiVersion: apps/v1
kind: Deployment
metadata:
  name: ocidex-scanner-worker
spec:
  template:
    spec:
      containers:
        - name: scanner-worker
          imagePullPolicy: IfNotPresent
          env:
            - name: NATS_STREAM_REPLICAS
              value: "1"
