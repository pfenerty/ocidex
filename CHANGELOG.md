# Changelog

All notable changes to this project will be documented in this file.

Entries are generated by [git-cliff](https://git-cliff.org) from conventional commits.
Run `make release VERSION=vX.Y.Z` to cut a release.

## [0.0.1-rc.1]

### Features

- Add registry polling support
- Add explicit repositories field for registries without catalog API
- Add enrichment sufficiency gating and OCI ref.name extraction
- Decouple API from frontend proxy, make API standalone
- **ci**: Update tektonic, add GitPipeline and caching
- **ci**: Switch to apko-cicd images, nushell scripts, remove NFS caching
- **ci**: Re-add zstd-compressed NFS cache for Go/Node, bump compute resources
- Add missing DB indexes and extract license_category() function
- Add RequireRegistryOwner middleware for registry write endpoints
- Surface registry visibility toggle and owner in admin UI
- Pluggable enricher registry with user enricher
- **scanner**: Synchronous NATS publish on API webhook path (ocidex-6ce.47)
- **nats**: Nats-Msg-Id dedup on submit + relay (ocidex-6ce.46)
- **jobs**: Scan_jobs table + lifecycle service (ocidex-6ce.49)
- **workers**: Add --once mode for scanner-worker and enrichment-worker
- **service**: Poller leader election via pg advisory lock (ocidex-6ce.51)
- **auth**: Add read/read-write scopes to API keys (ocidex-6ce.30)
- **api**: Paginate list-registries + fix RFC 7807 error consistency (ocidex-6ce.28)
- **frontend**: Move metrics dashboard to /admin/metrics
- **frontend**: Version nav flow + component sort default
- **frontend**: Add View build history button to SBOM detail page
- **frontend**: Move Add/Edit Registry form into modal dialog
- **frontend**: Add scan jobs UI to admin panel
- **frontend**: Apply visual identity refresh — field guide / entry cards
- **admin**: Expand system status tab with scan job counts and DB latency
- **frontend**: Public-facing landing page at /
- **registry**: Enforce type capabilities for scan mode and URL
- **ui**: Collapse packages/deps tabs into single tree-first view
- **ui**: Polish tree view — highlight toggle, column parity, smooth animation, collapse/expand all
- **ui**: Add 'Show plan files' toggle to package and diff views
- **ui**: Tree-structured diff view in build history
- **api+ui**: Add diff-tree endpoint; eliminate 94% of dep graph payload
- **ui**: Filter buttons, color-coded rows, hide no-purl context nodes
- **ui**: Unified tree/list diff view across all comparison pages
- **ui**: Address improvements.md UX items in artifact view
- **audit**: Emit login/logout events through audit extension
- **ui**: Add state/repo/registry filter bar to Jobs tab
- **nats**: Dead-letter stream and scan_job_failures table for exhausted scan jobs
- **nats**: Route catalog walks through NATS to scanner-worker
- **k8s**: Convert to kustomize base + overlays/dev
- **ui**: Richer SBOM picker labels with arch and build time on diff page
- **ui**: Tailwind migration final sweep — extract utility aliases and shared primitives
- **diff**: Backend-compute direction, nodeRef, roots, isDirect, descendantChanges (B1-B6)
- **openapi**: Regenerate spec and TS types for B1-B6 (B7)
- **diff**: Per-ancestor rollup chips driven by descendantChanges (C3, bqh.17)
- **deps+diff**: Backend roots for DependencyGraph; color-blind badge palette; diff fallback (A2, A4, C4)
- **frontend**: Add expand-all, show-context, show-transitive controls to diff tree (bqh.21-23)
- **db**: Add sbom.flavor column and index (bqh.28)
- **service**: Detect SBOM flavor at ingest via 3-layer algorithm (bqh.29)
- **service**: Flavor-aware changelog grouping + backfill cmd (bqh.30-32)
- **frontend**: Flavor tab strip in ChangelogTab + flavor-aware labels (bqh.33)
- **frontend**: G1 default tree shows descendants of changed direct deps (bqh.35)
- **frontend**: D6 persist diff view-mode and toggle prefs in localStorage (bqh.24)
- **frontend**: Cross-arch warning banner on Diff page (e5b.3)
- **frontend**: Couple 'To' picker arch to 'From' SBOM by default (e5b.5)
- **frontend**: Collapsible diff cards, layout fix, toggle tooltips (5fo, oaj, an1)
- **dev**: Local Talos + Tilt cluster for tight K8s dev loop (pww)
- **dev**: Make the dev stack reachable over Tailscale
- **dev**: Unify auth config — Tilt reads .env, base manifests use envFrom
- **release**: Tag-driven release pipeline with git-cliff and OCI annotations
- Gitcliff

### Bug fixes

- Update CI to Go 1.25.x to match go.mod toolchain
- Update test NewRouter calls to pass apiBaseURL argument
- Resolve all golangci-lint errors
- Resolve remaining golangci-lint errors
- Replace *bool query param with string to fix huma v2 panic
- **test**: Update SBOM route paths from /sbom to /sboms
- **test**: Update sbom_test.go routes from /sbom to /sboms
- **web**: Resolve ESLint errors and TypeScript build failures from OpenAPI spec update
- **ci**: Fix cache permissions for NFS all_squash (anonuid=1024)
- **ci**: Run go test per-package for incremental logging
- **ci**: Stream command output in real time, remove go list buffering
- **ci**: Reduce go-test memory limit to 2Gi (node allocatable ~3.1Gi)
- **ci**: Move computeResources to step level, not stepTemplate
- **ci**: Update tektonic for cache hash fix
- **ci**: Lower go-test/build memory request to 256Mi for scheduling
- Harden CORS defaults and replace unsafe HTTP clients
- Handle ignored json.Marshal/Unmarshal errors
- Add resource limits and imagePullPolicy to K8s deployment
- Use PATCH (not PUT) for registry update API call
- **scanner**: Block on Submit instead of dropping when queue is full
- **nats**: Move Ack to after processing to restore at-least-once delivery (ocidex-6ce.45)
- **frontend**: Center modal dialog and style password/number inputs
- **scanner**: Track jobs in in-process dispatcher mode
- **scanner**: Eliminate double Start and swallowed finishJob errors
- **api**: Enforce auth on SBOM ingest and SBOM/artifact deletion (ocidex-6ce.37)
- **admin**: Expose poller status and filter polling table to poll-mode registries
- **scanner**: Warn on unpolled registries and set finished_at on job failure
- **api**: Reject poll/both scan_mode when registry poller is disabled
- **scanner**: Prevent stuck jobs and state overwrites on NATS redelivery
- **frontend**: Hide poll/both scan modes when registry poller is disabled
- **frontend**: Always show all registry types; warn when poller required
- **api**: Introduce versions endpoint; fix artifact tab pagination
- **ui**: Lazy-mount tree children to prevent browser crash
- **ui**: Add missing showPlanFiles prop to ChangelogTab's DiffEntry call
- **ui**: Correct plan-file filter issues in search bar and diff views
- **ui**: Require purl in build history diff to exclude file-type entries
- **ui**: Use named import for DiffTreeView
- **ui**: Default diff tree to collapsed; expand one level at a time
- **ui**: Drop badge-sm from change badge; badge-sm overrides color classes
- **ui**: Use blue/amber badge colors for colorblind accessibility
- **api+ui**: Populate architecture/buildDate in diff-tree SBOMRef; add summary badges
- **api**: Reconcile version-suffixed package name changes in diff
- **ui**: Keep job error detail open across query refetches
- **nats**: Add per-message timeout and lifecycle logs to workers
- **scanner**: Strip URL scheme from registry host in normalizeRegistryHost
- **enrichment,ui**: Deduplicate Windows multi-arch architecture labels
- **ui**: Hide webhook actions for poll-only registries
- **changelog**: Use ListSBOMPackages in GetArtifactChangelog to drop file noise
- **diff**: Purl qualifier filtering and versioned-package survivor guard (E1, E2)
- **service**: Normalize distro qualifier to family in identity key (e5b.2)
- **frontend**: Render every root in DiffTreeView, including disconnected ones (e5b.1)
- **test**: Migrate integration tests to auth + sufficient=false (t2k)
- **service**: Backfill UNKNOWN Go main-module version from subjectVersion (ldu)
- **db**: Make migration 00026 fast on large component tables (3d4)
- **dev**: Adapt to talosctl cluster create syntax change
- **dev**: Refuse root and tolerate talosctl coredns timeout
- **dev**: Make dev-cluster-down idempotent and thorough
- **dev**: Preflight br_netfilter and auto-merge kubeconfig
- **dev**: Make the local Talos+Tilt stack actually come up green
- **dev**: Preserve Host header through Vite proxy for OAuth redirect
- **dev**: Prune stale talos/kube contexts on dev-cluster-down
- **dev**: Clean up stale tilt processes in dev-up/dev-down
- **dev**: Stop docker-compose stack before starting Tilt

### Refactor

- Normalize API endpoint structure for standalone service use
- Fix context propagation, consolidate NATS stream names, extract BuildInsecureResolver
- Convert Login.tsx to Tailwind, fix auth error handling, scope toast ID
- **scanner**: Replace discovererForType switch with registration pattern
- **scanner**: Extract Scanner interface, rename concrete type to SyftScanner
- **config**: Collapse NATS toggles to OCIDEX_MODE
- **ui**: Remove show-plan-files toggle; always filter file-type components
- **ui**: Remove tree filter buttons; color only on change badge
- **ui**: Decompose Admin.tsx into per-tab sub-pages
- **ui**: Decompose ImageMetadataCard into composable sub-modules
- **ui**: Migrate toast and landing page CSS out of index.css
- **ui**: Extract Layout and sidebar CSS to co-located component files
- **ui**: Extract page-scoped CSS to co-located component files
- **frontend**: Remove client-side classifyChange; use backend direction + roots (C1, C2, A3)
- **frontend**: Centralize diff tree expansion state in Set<ref> signal (bqh.19)
- **frontend**: Flatten diff tree into DFS row array, drop recursive component (bqh.20)
- **docker**: Split into per-service images (api, scanner, enrichment)

### Documentation

- Expand image labeling guide with full enrichment metadata coverage
- Add endpoint auth matrix and identify missing handler-level guards
- Require new branch from main when starting beads issues
- Remind Claude to run make openapi after API type changes
- Clarify goose DB migration tool vs goose AI agent
- **workflow**: Require implementation notes on beads issue close
- **adr**: Add 0023 visual identity — field guide / entry cards
- Add configuration reference, update .env.example and docker-compose
- **claude**: Refresh ADR table + roadmap epic IDs, add bqh epic
- **adr**: ADR-0019 diff identity model (bqh.1)
- **adr**: ADR-0020 image flavor detection from SBOM contents (bqh.2)
- **adr**: ADR-0021 backend-computed diff tree and rollups (bqh.3)
- SBOM diff user guide and CLAUDE.md cleanup (bqh.39, bqh.38)
- Codify TDD process for diff/tree changes (e5b.7)
- **env**: Note REGISTRY_POLLER_ENABLED is required for GHCR registration

### Tests

- Add OCI enricher integration tests with mock registry
- Add vitest frontend test suite and integrate into make check
- Add auth-boundary integration tests for admin/member/viewer/anon
- **service**: Unit tests for SBOM ingest paths (ocidex-6ce.9)
- **service**: Unit tests for search filters & visibility (ocidex-6ce.10)
- **service**: Unit tests for auth.go, registry.go, and helpers (ocidex-6ce.41)
- **api**: Add auth boundary tests (ocidex-6ce.11)
- **scanner**: Add unit tests for catalog walkers and discoverers (ocidex-6ce.12)
- **integration**: Add full scan-to-enrich pipeline test (ocidex-6ce.13)
- **frontend**: Add vitest component tests for Login, Layout, and Artifacts
- **nats**: Integration tests for Nats-Msg-Id dedup and retry-after-nak
- **service**: E3 diff identity edge cases — dotted suffix, multi-arch, cross-distro
- **service**: F7 selectFlavor + deduplicateSBOMs flavor-axis coverage (bqh.34)
- **service**: G2 fixture-driven tests for synthetic-root anchoring (bqh.36)
- **service**: Paired SBOM fixtures for distro-version drift (e5b.6)
- **integration**: Round-trip GET /api/v1/sboms/diff-tree (e5b.4)

### CI

- Add GHCR multi-arch image build workflow

### Chore

- Regenerate OpenAPI spec and TS types for polling fields
- Update web/package-lock.json
- Add openapi staleness check and GitHub Actions CI
- **ci**: Optimize and enhance CI/CD pipeline
- **ci**: Pin GitHub Actions to commit hashes
- **deps-dev**: Bump vite from 6.4.1 to 7.3.1 in /web
- **web**: Update package-lock.json after openapi regen
- **flox**: Pin tilt/talosctl/kubectl for the local K8s dev loop (dj2)

### Revert

- **ui**: Tree back to simple table; punt animation/expand-all

