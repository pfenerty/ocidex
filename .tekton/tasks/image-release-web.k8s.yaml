apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: ocidex-image-release-web
  namespace: ocidex-ci
spec:
  params:
    - name: repo-full-name
      type: string
    - name: revision
      type: string
  stepTemplate:
    computeResources:
      limits:
        cpu: "1"
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 128Mi
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
    workingDir: $(workspaces.workspace.path)
  steps:
    - computeResources:
        limits:
          cpu: "4"
          memory: 4Gi
        requests:
          cpu: 500m
          memory: 1Gi
      env:
        - name: DOCKER_CONFIG
          value: /tmp/docker-auth
        - name: BUILDKITD_FLAGS
          value: --oci-worker-snapshotter=native --oci-worker-no-process-sandbox
      image: moby/buildkit:rootless
      name: build-and-push
      onError: continue
      script: |
        #!/bin/sh
        TAG="$(params.source-branch)"
        BARE="${TAG#v}"
        MAJOR="$(echo "${BARE}" | cut -d. -f1)"
        MINOR="$(echo "${BARE}" | cut -d. -f2)"

        NAMES="ghcr.io/pfenerty/ocidex-web:${TAG},ghcr.io/pfenerty/ocidex-web:${MAJOR}.${MINOR},ghcr.io/pfenerty/ocidex-web:${MAJOR}"
        if ! echo "${TAG}" | grep -q '-'; then
          NAMES="${NAMES},ghcr.io/pfenerty/ocidex-web:latest"
        fi

        buildctl-daemonless.sh build \
          --frontend dockerfile.v0 \
          --local context=. \
          --local dockerfile=. \
          --opt filename=docker/web/Dockerfile \
          --opt platform=linux/amd64,linux/arm64 \
          --opt build-arg:VERSION="${TAG}" \
          --opt build-arg:COMMIT="$(params.revision)" \
          --opt build-arg:DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
          --opt attest:provenance=mode=max \
          --opt attest:sbom= \
          --output "type=image,\"name=${NAMES}\",push=true,attestation-manifest-referrers=true"
        ec=$?
        echo "${ec}" > /tekton/home/.exit-code
        exit "${ec}"
      securityContext:
        allowPrivilegeEscalation: true
        capabilities:
          add:
            - SETUID
            - SETGID
          drop: []
        runAsGroup: 1000
        runAsUser: 1000
        seccompProfile:
          type: Unconfined
      volumeMounts:
        - mountPath: /tmp/docker-auth/config.json
          name: docker-config
          readOnly: true
          subPath: .dockerconfigjson
      workingDir: $(workspaces.workspace.path)
    - env:
        - name: GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              key: token
              name: github-token
      image: ghcr.io/pfenerty/apko-cicd/base:stable
      name: report-status
      script: |-
        #!/usr/bin/env nu
        def log [msg: string] {
          print $"[(date now | format date '%H:%M:%S')] report-status [image-release-web]: ($msg)"
        }

        let exit_code = (try { open --raw /tekton/home/.exit-code | str trim | into int } catch { 1 })
        let state = if $exit_code == 0 { "success" } else { "failure" }
        let desc = if $exit_code == 0 { "Passed" } else { "Failed" }

        log $"exit-code=($exit_code) state=($state)"

        let url = $"https://api.github.com/repos/$(params.repo-full-name)/statuses/$(params.revision)"
        let body = { state: $state, context: "image-release-web", description: $desc }

        log $"POST ($url)"
        log $"body: ($body | to json -r)"

        try {
          http post $url $body -t application/json -H [
            Authorization $"token ($env.GITHUB_TOKEN)"
            Accept "application/vnd.github+json"
          ]
          log "done"
        } catch { |e|
          log $"error: ($e.msg)"
        }
  volumes:
    - name: docker-config
      secret:
        secretName: ghcr-docker-config
  workspaces:
    - name: workspace
