apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: ocidex-image-release-operator
  namespace: ocidex-ci
spec:
  params:
    - name: repo-full-name
      type: string
    - name: revision
      type: string
  stepTemplate:
    computeResources:
      limits:
        cpu: "1"
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 128Mi
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
    workingDir: $(workspaces.workspace.path)
  steps:
    - computeResources:
        limits:
          cpu: "4"
          memory: 4Gi
        requests:
          cpu: 500m
          memory: 1Gi
      env:
        - name: DOCKER_CONFIG
          value: /tmp/docker-auth
        - name: BUILDKITD_FLAGS
          value: --oci-worker-snapshotter=native --oci-worker-no-process-sandbox
        - name: IMAGE
          value: ghcr.io/pfenerty/ocidex-operator
        - name: DOCKERFILE
          value: docker/Dockerfile
        - name: TARGET
          value: operator
      image: moby/buildkit:rootless
      name: build-and-push
      onError: continue
      script: |-
        #!/bin/sh
        log() { printf '[%s] %s\n' "$(date +%H:%M:%S)" "$*"; }
        (
        # Release build: tags images with semver aliases from the git tag name.
        # latest is only added for stable releases (no hyphen in tag).
        # Per-image values come from step env: DOCKERFILE, IMAGE, and optional TARGET.
        # buildctl is the final command so its exit code is what synth captures.
        TAG="$(params.source-branch)"
        TAG="${TAG#refs/tags/}"
        BARE="${TAG#v}"
        MAJOR=$(echo "$BARE" | cut -d. -f1)
        MINOR=$(echo "$BARE" | cut -d. -f2)

        NAMES="$IMAGE:$TAG,$IMAGE:$MAJOR.$MINOR,$IMAGE:$MAJOR"
        if ! echo "$TAG" | grep -q '-'; then
          NAMES="$NAMES,$IMAGE:latest"
        fi

        TARGET_OPT=""
        if [ -n "$TARGET" ]; then TARGET_OPT="--opt target=$TARGET"; fi

        buildctl-daemonless.sh build \
          --frontend dockerfile.v0 \
          --local context=. \
          --local dockerfile=. \
          --opt filename="$DOCKERFILE" \
          $TARGET_OPT \
          --opt platform=linux/amd64,linux/arm64 \
          --opt build-arg:VERSION="$TAG" \
          --opt build-arg:COMMIT="$(params.revision)" \
          --opt build-arg:DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
          --opt attest:provenance=mode=max \
          --opt attest:sbom= \
          --export-cache "type=registry,ref=$IMAGE:buildcache,mode=max,image-manifest=true,oci-mediatypes=true" \
          --import-cache "type=registry,ref=$IMAGE:buildcache" \
          --output "type=image,\"name=$NAMES\",push=true,attestation-manifest-referrers=true"
        )
        __tek_rc=$?
        __tek_prev=$(cat /tekton/home/.exit-code 2>/dev/null || echo 0)
        if [ "$__tek_rc" -gt "$__tek_prev" ]; then __tek_prev="$__tek_rc"; fi
        printf '%s' "$__tek_prev" > /tekton/home/.exit-code
        exit "$__tek_rc"
      securityContext:
        allowPrivilegeEscalation: true
        capabilities:
          add:
            - SETUID
            - SETGID
          drop: []
        runAsGroup: 1000
        runAsUser: 1000
        seccompProfile:
          type: Unconfined
      volumeMounts:
        - mountPath: /tmp/docker-auth/config.json
          name: docker-config
          readOnly: true
          subPath: .dockerconfigjson
      workingDir: $(workspaces.workspace.path)
    - env:
        - name: GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              key: token
              name: github-pipeline-token
      image: ghcr.io/pfenerty/apko-cicd/base:stable
      name: report-status
      script: |-
        #!/usr/bin/env nu
        def log [msg: string] { print $"[(date now | format date '%H:%M:%S')] ($msg)" }
        let exit_code = (try { open --raw /tekton/home/.exit-code | str trim | into int } catch { 1 })
        let state = if $exit_code == 0 { "success" } else { "failure" }
        let desc = if $exit_code == 0 { "Passed" } else { "Failed" }

        log $"report-status [image-release-operator]: exit-code=($exit_code) state=($state)"

        let url = $"https://api.github.com/repos/$(params.repo-full-name)/statuses/$(params.revision)"
        let body = { state: $state, context: "image-release-operator", description: $desc }

        log $"report-status [image-release-operator]: POST ($url)"
        log $"report-status [image-release-operator]: body: ($body | to json -r)"

        try {
          http post $url $body -t application/json -H [
            Authorization $"token ($env.GITHUB_TOKEN)"
            Accept "application/vnd.github+json"
          ]
          log "report-status [image-release-operator]: done"
        } catch { |e|
          log $"report-status [image-release-operator]: error: ($e.msg)"
        }
  volumes:
    - name: docker-config
      secret:
        secretName: ghcr-docker-config
  workspaces:
    - name: workspace
